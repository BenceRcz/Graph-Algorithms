#include <stack>
#include <queue>
#include "Utils.h"

using namespace std;

// This function implements the Depth first search algorithm in an iterative manner
void dfs_itr(const vector<vector<ui>>& adj_list, const ui& n, const ui& m) {
	bool* visited = new bool[n] {0};
	stack<ui> stack;

	for (ui i = 0; i < n; ++i) {

	}
}

int main() {
	ui n, m;
	//vector<vector<ui>> adj_list = read_unweighted_list("", n, m);
	return 0;
}