#include "Utils.h"

// This function implements kruskals algorithm to find an mst
// returns the mst of a given graph
void mst_kruskal() {
}

int main() {
	ui n, m;
	vector<link> edje_list = read_weighted_edje_list("weighted_graph1.in", n, m);
	cout << "1";
	return 0;
}